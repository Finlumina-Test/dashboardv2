# Environment Configuration Example
# Copy this file to .env.local and update with your values

# Base URL for Create.xyz (if needed)
# NEXT_PUBLIC_CREATE_BASE_URL=https://www.create.xyz

# Project Group ID (if needed for Create.xyz integration)
# NEXT_PUBLIC_PROJECT_GROUP_ID=your-project-id-here

# Server Configuration
PORT=4000
NODE_ENV=development

# Note: Restaurant configurations are managed in src/utils/restaurantConfig.js
# Update that file to configure restaurant backends, usernames, and passwords
